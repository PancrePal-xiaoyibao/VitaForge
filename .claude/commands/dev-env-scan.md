---
description: 开发环境扫描与偏好配置 — 检测工具链+收集偏好+输出 profile
---

扫描当前开发环境（OS、语言运行时、包管理器、GPU、容器等），交互式收集开发者偏好（主力语言、部署风格、测试哲学等），输出 `.dev-profile.json` + 更新 CLAUDE.md 的 `## Developer Environment` 段落。

$ARGUMENTS

## 关联 Skill（网络调度协议）

| 用户意图关键词 | 路由目标 | 说明 |
|---------------|---------|------|
| 环境扫描 / 新项目 / onboarding / dev scan / 偏好设置 | → 本命令 | 开发环境扫描与偏好配置 |

| 关系 | Skill | 场景 |
|------|-------|------|
| 被调用 | ai-spec | 新项目时主调度路由 |
| 输出供 | ai-spec | 环境信息供技术栈决策 |
